spring.application.name=campaign-assessment

# In production this would be injected via environment variable or a secrets manager.
api.key=EXAMPLE-KEY
api.rate-limit.requests-per-minute=10

# Swagger UI available at /swagger-ui.html
springdoc.swagger-ui.path=/swagger-ui.html
